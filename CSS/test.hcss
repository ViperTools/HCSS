$primary = white
$secondary = blue
$font = Arial, sans-serif
$test = {
	abc: def;
}

/* div, section {
	color: $primary;
	font: $font;
	&:hover {
		color: red;
	}
	& p {
		color: $secondary;
	}
}

div $test */